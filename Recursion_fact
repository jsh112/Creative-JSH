#include <stdio.h>

int factorial(int n){
  if(n <= 1){
    return 1;
  }
  return n * factorial(n - 1);
}


int main(void){


  int k;
  scanf("%d", &k);
  factorial(k);
  printf("%d factorial is %d",k,factorial(k));
  return 0;
}

